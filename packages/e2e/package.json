{
  "name": "@pokertools/e2e",
  "version": "1.0.17",
  "description": "End-to-end Docker integration tests for PokerTools API",
  "author": "A.Aurelius",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "pretest:docker": "echo '🐳 Building Docker E2E dependencies...' && npm run build -w @pokertools/types && npm run build -w @pokertools/sdk && npm run contracts:build -w @pokertools/admin --if-present",
    "test:docker": "NODE_ENV=test vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@pokertools/sdk": "1.0.17",
    "@prisma/adapter-better-sqlite3": "^7.10.0",
    "@prisma/client": "^7.10.0",
    "@scure/bip32": "^2.4.0",
    "@scure/bip39": "^2.4.0",
    "better-sqlite3": "^13.0.3",
    "bullmq": "^6.3.4",
    "dotenv": "^17.4.2",
    "fast-jwt": "^6.3.3",
    "ioredis": "^6.0.0",
    "viem": "^2.56.3",
    "vitest": "^5.0.0",
    "ws": "^8.21.3"
  }
}
